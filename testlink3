<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitHub Pages Test</title>
</head>
<body>
    <h2>GitHub Pages is Working ✅</h2>
    <p>If you see this text, your GitHub Pages is properly configured.</p>

    <script>
        alert("✅ JavaScript is running!");

        let urlParams = new URLSearchParams(window.location.search);
        let payload = urlParams.get("payload");

        if (payload) {
            document.body.innerHTML += `<h3>Payload: ${payload}</h3>`;
        }
    </script>
</body>
</html>
